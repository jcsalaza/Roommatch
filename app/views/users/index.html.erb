<h1>All Users</h1>
<% @users.each do |user| %>
<div>
  <%= "This is #{user.first_name} #{user.last_name}" %>
  <%= user.school %>
  <%= user.city %>
  <%= link_to "See More", user_path(user) %>
</div>
<% end %>